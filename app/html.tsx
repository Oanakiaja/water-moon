import { Html } from "@react-three/drei";

const HtmlContent = () => {
  return (
    <Html fullscreen>
      <div
        style={{
          position: "absolute",
          top: 0,
          left: 0,
        }}
      >
        {/* TODO： 海上生明月 */}
      </div>
    </Html>
  );
};

export default HtmlContent;
